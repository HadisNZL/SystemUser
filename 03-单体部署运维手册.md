# 单体部署运维手册

本文档记录当前项目在本地或 Linux 服务器上的基础部署、启动、排查命令。

## 一、部署方式

当前项目支持两种运行方式：

```text
方式一：Java Jar 运行
方式二：Docker Compose 运行后端应用 + MySQL + Redis + RabbitMQ + Nacos
```

学习阶段优先掌握 Docker Compose 方式，同时保留 Jar 运行能力。

## 二、Java Jar 运行

打包：

```bash
./mvnw clean package -DskipTests
```

启动：

```bash
java -jar target/admin-system-0.0.1-SNAPSHOT.jar
```

指定环境：

```bash
java -jar target/admin-system-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev
```

后台运行：

```bash
nohup java -jar target/admin-system-0.0.1-SNAPSHOT.jar --spring.profiles.active=dev > app.log 2>&1 &
```

查看后台日志：

```bash
tail -f app.log
```

## 三、Docker Compose 运行

只启动中间件，本机运行后端：

```bash
./scripts/app.sh middleware
./mvnw spring-boot:run
```

启动后端应用和中间件：

```bash
./scripts/app.sh start
```

查看服务状态：

```bash
./scripts/app.sh ps
docker compose ps
```

查看后端日志：

```bash
./scripts/app.sh logs
docker compose logs -f admin-system
```

停止服务：

```bash
./scripts/app.sh stop
```

不要随手执行：

```bash
docker compose down -v
```

这个命令会删除 Docker named volume，MySQL、Redis、RabbitMQ 和上传文件数据都会被删除。

## 四、健康检查

应用健康检查地址：

```text
http://localhost:8080/actuator/health
```

正常响应：

```json
{"status":"UP"}
```

命令行测试：

```bash
curl http://localhost:8080/actuator/health
```

## 五、进程与端口排查

查看 Java 进程：

```bash
ps -ef | grep java
```

查看 8080 端口：

```bash
lsof -i :8080
```

查看 MySQL / Redis / RabbitMQ 端口：

```bash
lsof -i :3306
lsof -i :6379
lsof -i :5672
lsof -i :15672
lsof -i :8848
```

查看 Docker 容器：

```bash
docker ps
docker compose ps
```

## 六、日志排查

查看后端容器日志：

```bash
docker compose logs -f admin-system
```

查看 MySQL 日志：

```bash
docker compose logs -f mysql
```

查看 Redis 日志：

```bash
docker compose logs -f redis
```

查看 RabbitMQ 日志：

```bash
docker compose logs -f rabbitmq
```

查看 Nacos 日志：

```bash
docker compose logs -f nacos
```

常见启动成功标志：

```text
Started AdminSystemApplication
The following 1 profile is active: "docker"
```

## 七、环境变量

Docker 运行后端时，关键环境变量在 `docker-compose.yml` 中：

```text
SPRING_PROFILES_ACTIVE=docker
DB_PWD=123456
RABBITMQ_USER=guest
RABBITMQ_PWD=guest
JWT_SECRET=abc123456abc123456abc123456abc123456
JWT_EXPIRE=7200
```

Jar 运行时可以通过命令行参数或环境变量覆盖配置。

## 八、数据库备份与恢复

备份 Docker MySQL 数据库：

```bash
docker exec admin-mysql mysqldump -uroot -p123456 --single-transaction --set-gtid-purged=OFF admin_system > docker/mysql/backup/admin_system.sql
```

恢复数据库：

```bash
docker exec -i admin-mysql mysql -uroot -p123456 admin_system < docker/mysql/backup/admin_system.sql
```

备份文件默认不提交 Git。

## 九、基础排查顺序

接口访问异常时，按这个顺序查：

```text
1. docker compose ps 看容器是否都在
2. curl /actuator/health 看后端是否存活
3. docker compose logs -f admin-system 看后端日志
4. 检查 MySQL / Redis / RabbitMQ 容器日志
5. 检查端口是否被其他进程占用
6. 检查 application-dev.yaml 或 application-docker.yaml 配置
```
