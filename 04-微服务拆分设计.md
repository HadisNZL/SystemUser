# 微服务架构设计

本文档记录 `admin-system` 完成单体退场后的服务边界、调用关系和关键技术决策。数据库当前保持共享，根工程仅承担 Maven 聚合职责。

## 一、整体架构

```text
前端 / Knife4j
       |
       v
gateway-service:9000
  |-- /auth/**   -> auth-service:9101
  |-- /system/** -> system-service:9201
  |-- /file/**   -> file-service:9401
  `-- /log/**    -> log-service:9301

服务注册与发现：Nacos
服务间同步调用：OpenFeign + Spring Cloud LoadBalancer
异步操作日志：RabbitMQ
缓存与令牌黑名单：Redis
业务数据：MySQL admin_system
```

外部请求统一访问 `gateway-service:9000`。业务服务端口用于本地调试和健康检查，不作为前端正式入口。

## 二、模块职责

| 模块 | 类型 | 职责 |
| --- | --- | --- |
| `admin-common` | 公共 Jar | 统一响应、错误码、业务异常、公共常量和日志消息模型 |
| `admin-operation-log` | 公共 Jar | 操作日志注解、AOP 切面、RabbitMQ 消息发送 |
| `gateway-service` | 应用服务 | 路由、跨域、JWT 校验、Token 黑名单、身份透传、请求日志、接口文档聚合 |
| `auth-service` | 应用服务 | 验证码、登录、JWT 签发、退出登录、认证限流 |
| `system-service` | 应用服务 | 用户、角色、菜单、字典、RBAC、当前用户资料、内部认证授权查询 |
| `file-service` | 应用服务 | 文件上传下载、文件元数据、文件操作日志 |
| `log-service` | 应用服务 | 操作日志消费、查询和定时清理 |

根目录不再存在单体 `src`，也不再生成或启动 `admin-system` Jar。

## 三、依赖边界

```text
admin-common
  -> 被业务服务复用

admin-operation-log
  -> 依赖 admin-common
  -> 被 system-service、file-service 复用

gateway-service
  -> 不依赖业务服务代码
  -> 通过 Nacos 和路由访问业务服务

auth-service
  -> 通过 OpenFeign 调用 system-service 内部认证接口

file-service / log-service
  -> 通过 OpenFeign 调用 system-service 内部授权接口
```

公共模块以普通 Maven Jar 参与编译，不单独启动，也不注册到 Nacos。只有五个应用服务拥有启动类和运行端口。

## 四、主要调用链路

登录链路：

```text
POST /auth/login
  -> gateway-service 放行登录请求
  -> auth-service 校验验证码
  -> OpenFeign 调用 system-service /internal/auth/login-user
  -> 校验 BCrypt 密码
  -> auth-service 签发 JWT
```

业务接口链路：

```text
GET /system/user/{id}
  -> gateway-service 校验 JWT 和 Token 黑名单
  -> 解析 userId
  -> 透传 X-Gateway-Forwarded / X-User-Id
  -> system-service 恢复 SecurityContext
  -> @PreAuthorize 校验 permission_key
  -> 执行业务查询
```

文件和日志服务鉴权链路：

```text
gateway-service 透传 userId
  -> file-service / log-service
  -> OpenFeign 调用 system-service /internal/authorization/users/{userId}
  -> 加载用户状态和权限
  -> 恢复 SecurityContext
  -> 执行业务接口
```

操作日志链路：

```text
system-service / file-service 写操作
  -> @OperationLog
  -> admin-operation-log AOP
  -> RabbitMQ exchange + routing key
  -> log-service 监听 queue
  -> sys_operation_log
```

日志发送失败只记录错误，不回滚主业务。

## 五、认证与信任边界

### 1. 网关职责

网关统一校验：

```text
Authorization: Bearer <token>
```

校验成功后生成：

```text
X-Gateway-Forwarded: true
X-User-Id: 当前用户ID
```

下游服务不再重复解析 JWT，只根据网关身份头恢复用户和权限。

### 2. 下游服务职责

`system-service`、`file-service`、`log-service` 继续使用 Spring Security 和 `@PreAuthorize` 做方法级权限校验。前端按钮隐藏不能替代后端权限校验。

`GatewayForwardAuthenticationFilter` 只允许在 Spring Security 过滤器链中执行。三个服务均通过 `FilterRegistrationBean#setEnabled(false)` 禁止 Servlet 容器重复注册，否则 `OncePerRequestFilter` 可能被提前执行并导致认证上下文丢失。

### 3. 访问约束

正式业务请求必须经过 `9000` 网关。直接访问 `9201/9301/9401` 不会携带可信网关身份，受保护接口应返回 401。

## 六、RBAC 权限模型

```text
sys_user
  -> sys_user_role
  -> sys_role
  -> sys_role_permission
  -> sys_permission.permission_key
  -> GrantedAuthority
  -> @PreAuthorize
```

目录和菜单用于前端路由展示，按钮类型权限的 `permission_key` 用于接口鉴权。角色、菜单或用户角色关系变化后，需要清理对应用户的 Redis 权限缓存。

## 七、数据与事务边界

当前五个服务共享一个 `admin_system` 数据库。这是现阶段的明确选择：

- 先完成代码和运行单元拆分。
- 保留现有 RBAC 表关系。
- 避免过早引入分布式事务。
- 每个服务只操作自己职责范围内的表。

当前主要归属：

| 服务 | 数据表 |
| --- | --- |
| `system-service` | `sys_user`、`sys_role`、`sys_permission`、关联表、字典表 |
| `file-service` | `sys_file` |
| `log-service` | `sys_operation_log` |
| `auth-service` | 不直接操作业务表，通过 OpenFeign 查询用户 |

后续只有在独立扩缩容、数据隔离或团队边界明确时，才考虑按服务拆库。

## 八、服务发现与配置

五个应用服务注册到 Nacos：

```text
gateway-service
auth-service
system-service
file-service
log-service
```

网关使用 `lb://服务名` 路由，OpenFeign 也通过服务名调用，不写死业务服务 IP。`dev` 和 `docker` profile 分别使用宿主机地址与 Compose 服务名。

网关已接入 Nacos Config，开发环境 Data ID 为：

```text
gateway-service-dev.yaml
```

## 九、接口文档

统一入口：

```text
http://localhost:9000/doc.html
```

Knife4j 聚合四组 OpenAPI 文档：

```text
认证服务 /v3/api-docs/auth
系统服务 /v3/api-docs/system
文件服务 /v3/api-docs/file
日志服务 /v3/api-docs/log
```

每个 Controller 使用 `@Tag` 标注业务模块，每个接口使用 `@Operation` 标注摘要和说明。生产 profile 默认关闭接口文档。

## 十、运行方式

本地开发：

```bash
./scripts/dev.sh start
./scripts/dev.sh status
./scripts/dev.sh stop
```

完整 Docker：

```bash
./scripts/app.sh start
./scripts/app.sh ps
./scripts/app.sh stop
```

两种模式都会占用 `9000/9101/9201/9301/9401`，不要同时启动。部署和排错细节见 `03-微服务部署运维手册.md`。

## 十一、当前结论

已经完成：

- 单体源码和 8080 路由删除。
- 五个应用服务独立运行并注册 Nacos。
- 两个公共模块以 Jar 方式复用。
- 登录、RBAC、文件和日志能力全部迁移。
- 网关统一鉴权、跨域、路由和 Knife4j 聚合。
- Docker Compose 一键编排应用与中间件。

暂未引入：

- 服务独立数据库与分布式事务。
- Sentinel 熔断降级。
- OpenTelemetry 链路追踪。
- Prometheus / Grafana 监控。
- Kubernetes 和自动化发布流水线。

下一阶段优先补稳定性与可观测性，不继续为了拆分数量新增空壳服务。
