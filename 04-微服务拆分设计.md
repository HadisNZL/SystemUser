# 微服务拆分设计

本文档记录当前 `admin-system` 从单体项目演进到微服务架构的拆分思路。当前阶段先做设计和边界梳理，不直接拆库、不一次性拆完所有服务。

## 一、为什么现在不能直接硬拆

当前项目已经具备完整后台能力：

```text
登录认证
用户 / 角色 / 菜单 / 权限
文件上传下载
操作日志
Redis 缓存
RabbitMQ 异步日志
Docker Compose 部署
```

但是这些能力在单体里还有大量共享对象：

```text
统一返回 Result
统一异常 BusinessException
JWT 工具
Security 鉴权
RBAC 数据表
DTO / VO / Entity
Mapper 和数据库事务
```

如果现在直接拆成多个服务，容易遇到：

```text
公共代码重复
服务间接口不稳定
认证链路断裂
权限上下文传递不清楚
数据库事务边界混乱
排查成本上升
```

所以第一步应该先明确服务边界，再做最小可运行拆分。

## 二、推荐服务边界

### 1. gateway-service

职责：

```text
统一入口
路由转发
跨域处理
基础限流
统一鉴权入口
```

后续请求链路：

```text
前端 -> gateway-service -> 后端业务服务
```

### 2. auth-service

职责：

```text
登录
退出登录
验证码
JWT 签发
token 黑名单
当前用户认证信息
```

来自当前单体的模块：

```text
LoginController
CaptchaController
LoginService
CaptchaService
JwtUtil
JwtAuthenticationFilter 的部分能力
TokenBlacklistService
```

### 3. system-service

职责：

```text
用户管理
角色管理
菜单权限管理
字典配置
RBAC 权限查询
当前用户资料
当前用户菜单
```

来自当前单体的模块：

```text
SysUserController
SysRoleController
SysMenuController
SysDictController
SysProfileController
用户 / 角色 / 菜单 / 权限相关 Service、Mapper、Entity
```

### 4. file-service

职责：

```text
文件上传
文件下载
文件元数据管理
本地存储或对象存储适配
```

来自当前单体的模块：

```text
SysFileController
SysFileService
SysFileMapper
SysFile
```

### 5. log-service

职责：

```text
操作日志消费
操作日志查询
历史日志清理
```

来自当前单体的模块：

```text
OperationLogMessageConsumer
OperationLogCleanTask
SysOperationLog
SysOperationLogMapper
```

注意：`OperationLogAspect` 更适合留在公共能力中，或者各业务服务各自接入。

## 三、第一阶段最小拆分目标

第一阶段不要一次性拆出所有服务，推荐先做：

```text
gateway-service
auth-service
system-service
```

原因：

```text
登录认证是入口
用户角色权限是后台核心
网关能体现微服务统一入口
文件和日志可以暂时留在单体或后续拆
```

第一阶段目标不是功能多，而是跑通：

```text
Nacos 注册
Gateway 路由
auth-service 登录签发 token
system-service 用户列表接口
Gateway 转发到下游服务
```

## 四、数据库拆分策略

初期不拆数据库。

推荐先使用：

```text
一个 MySQL 实例
一个 admin_system 数据库
多个服务暂时访问同一个库
```

原因：

```text
学习阶段先理解服务拆分
避免过早引入分布式事务
保留现有 RBAC 表关系
减少迁移成本
```

后续成熟后再考虑：

```text
auth_db
system_db
file_db
log_db
```

## 五、公共模块规划

拆分微服务前，需要先考虑公共模块：

```text
common-core       Result、ResultCode、BusinessException、常量
common-security   JWT 工具、当前用户上下文、权限注解支持
common-web        全局异常、Jackson、参数校验配置
common-log        操作日志注解、日志消息 DTO
common-mybatis    MyBatis-Plus 公共配置、自动填充
```

第一阶段可以先少拆公共模块，只抽最稳定的：

```text
common-core
common-security
```

## 六、认证与权限迁移思路

单体当前模式：

```text
客户端带 JWT
JwtAuthenticationFilter 校验 token
SecurityContext 保存当前用户
@PreAuthorize 判断权限
```

微服务推荐模式：

```text
客户端带 JWT
Gateway 校验 token
Gateway 解析用户 ID 和权限
Gateway 透传 X-User-Id / X-Permissions
下游服务读取可信请求头
下游服务继续使用 @PreAuthorize 或业务校验
```

注意：

```text
下游服务不能信任前端直接传来的 X-User-Id
这些请求头必须只由 Gateway 生成
```

## 七、推荐推进顺序

```text
1. 保留当前单体项目作为稳定版本
2. 新增多模块父工程或新建微服务工作区
3. 接入 Nacos 注册中心
4. 新建 gateway-service
5. 新建 auth-service，迁移登录和验证码
6. 新建 system-service，迁移用户列表接口
7. Gateway 配置路由到 auth-service / system-service
8. 打通登录 -> token -> 调用户列表
9. 再迁移角色、菜单、权限、字典
10. 最后再考虑 file-service 和 log-service
```

## 八、当前不做的事

当前阶段不做：

```text
不拆数据库
不做分布式事务
不一次性拆文件服务和日志服务
不急着上 Kubernetes
不为了微服务牺牲当前单体稳定性
```

先跑通最小闭环，再逐步增强。

## 九、Nacos 本地环境

当前项目已在 `docker-compose.yml` 中加入 Nacos：

```text
服务名：nacos
容器名：admin-nacos
镜像版本：nacos/nacos-server:v2.5.3
控制台：http://localhost:8848/nacos
容器内访问：http://nacos:8848
```

说明：`v2.5.3` 支持 Apple Silicon 的 `arm64` 镜像，本地 Mac 启动不会出现 `no matching manifest for linux/arm64/v8`。

当前单体项目已先接入 Nacos Discovery，启动后会以 `admin-system` 注册到 Nacos 服务列表。后续新建 `gateway-service`、`auth-service`、`system-service` 时，继续通过 Spring Cloud Alibaba 接入服务注册。

当前已新增 `gateway-service`，启动后会以 `gateway-service` 注册到 Nacos。第一版网关先把 `/sys/**`、`/captcha`、接口文档和健康检查请求通过 `lb://admin-system` 转发到当前单体后端。

## 十、Nacos 配置中心

当前单体项目已接入 Nacos Config。本地 `dev` 环境读取的 Data ID：

```text
admin-system-dev.yaml
```

可在 Nacos 控制台新增配置：

```yaml
system:
  info:
    name: admin-system
    description: Nacos配置中心测试
    version: 0.0.1-dev
```

验证接口：

```text
GET /sys/config/info
```

如果接口返回的 `description` 或 `version` 变成 Nacos 中配置的值，说明配置中心已经生效。

## 十一、Gateway 本地验证

本地开发启动顺序：

```bash
./scripts/dev.sh start
./scripts/dev.sh status
./scripts/dev.sh logs
./scripts/dev.sh stop
```

手动排错时，也可以分别启动中间件、单体后端和网关服务。

验证入口：

```text
POST http://localhost:9000/sys/login
GET  http://localhost:9000/sys/user/search_list?status=1&pageNum=1&pageSize=10
```

`9000` 是网关端口，`8080` 是当前单体后端端口。请求进入网关后，网关通过 Nacos 找到 `admin-system` 实例并转发。
