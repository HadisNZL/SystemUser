# 全套系统化学习日程表（全新现代写法 | 无老旧代码 | 严格对齐你的规划）

## 总阶段划分

第二阶段：**第 8-20 天 SpringBoot 核心 + 企业通用脚手架（项目地基）**

第三阶段：**第 21-32 天 两大企业实战项目（求职核心）**

第四阶段：**第 33-36 天 主流中间件实战（面试高分）**

第五阶段：**第 37-40 天 微服务架构 + 线上项目部署（入职高阶）**

------

# 第二阶段 第 8-20 天 详细每日课程表

## 第一部分：8~12 天 SpringBoot 框架精通（纯基础 不碰登录权限）

### 第 8 天 已完成

1. IDEA2026 新版创建 SpringBoot3 项目
2. Maven 依赖管理、依赖冲突解决
3. 项目标准分层架构搭建：`Controller / Service / Mapper / Entity`
4. YAML 核心配置编写、数据库基础连接配置
5. 三层架构完整打通，实现基础数据库查询接口
6. 项目启动流程、端口配置、上下文路径配置

### 第 9 天 已完成

1. 企业统一全局响应结果封装 `Result`
2. 全局统一异常处理器新版实现
3. 提前完成 MyBatis-Plus 环境整合（并入 13-16 天内容）
4. 接口统一返回格式规范化，前后端分离标准响应结构

### 第 10 天 SpringBoot 全类型参数接收 + RESTful 接口规范

1. 新版五种主流参数接收方式
   - 普通 URL 参数接收
   - 路径占位符参数 `@PathVariable`
   - JSON 实体接收 `@RequestBody`
   - DTO 专用入参接收（企业主流）
   - 表单提交参数接收
2. JSR380 新版参数全局校验注解使用
3. RESTful 风格接口严格规范
   - GET 查询、POST 新增、PUT 修改、DELETE 删除
   - 统一接口命名规范、请求方式规范
4. 接口注释规范、业务语义统一

### 第 11 天 全局跨域配置 + 组件注入 + 四大核心组件

1. 新版全局统一跨域配置（弃用局部注解跨域）
2. 依赖注入核心用法：`@Resource / @Autowired` 新版区别
3. SpringBoot 四大组件实战
   - 配置类`@Configuration`
   - 控制器`@RestController`
   - 业务组件`@Service`
   - 持久层组件`@Mapper`
4. 自定义 Bean 注册、Bean 优先级、组件生命周期

### 第 12 天 多环境配置 + 自动配置原理 + 全局日期统一处理

1. 企业多环境隔离配置
   - dev 开发环境、test 测试环境、prod 生产环境
   - 环境切换、配置分离、敏感配置隔离
2. SpringBoot3 新版自动配置核心原理极简吃透
3. 全局统一时间格式化、全局时区统一
4. 8~12 天全套知识点复盘 + 项目结构标准化定型

------

## 第二部分：13~16 天 MyBatis-Plus 全方位实战（纯数据层 只操作数据库）

### 第 13 天 MP 完整环境校验 + 实体类全套新版注解

1. 确认 MP 依赖、分页插件配置正常可用
2. 实体类企业全套注解实战
   - 主键注解 `@TableId`
   - 字段映射注解 `@TableField`
   - 表名映射注解 `@TableName`
3. BaseMapper 父接口内置所有通用 CRUD 方法全掌握
4. 无 XML 零 SQL 完成：新增、根据 ID 查询、修改、删除

### 第 14 天 MP 条件构造器全套实战（Lambda 优先 杜绝硬编码）

1. `LambdaQueryWrapper` 新版首选条件构造器
2. 等值、模糊、区间、排序、去重、空值判断
3. `QueryWrapper` 传统构造器使用场景区分
4. 多条件动态拼接查询、后台列表筛选标准写法

### 第 15 天 MP 高级实用功能

1. 新版**字段自动填充**（创建时间、修改时间自动赋值）
2. 全局**逻辑删除**配置 + `@TableLogic` 注解全局生效
3. MP 内置分页插件深度使用、分页返回结构封装
4. 自定义分页参数、分页默认值统一处理

### 第 16 天 MP 乐观锁 + 多表关联新版写法

1. 乐观锁 `@Version` 注解实现并发安全更新
2. MP 新版多表联查三种企业写法
   - Lambda 嵌套联查
   - 自定义 XML 简洁联查
   - 业务层组装数据联查
3. 13~16 天 MP 全套实战复盘，彻底告别手写基础 CRUD

------

## 第三部分：17~20 天 企业级通用脚手架整合（全套通用组件成型）

### 第 17 天 基础通用组件补齐

1. 完善全局跨域最终版配置
2. 项目通用常量类封装
3. 项目统一工具类封装（字符串、日期、集合、脱敏等）
4. 统一分页返回实体类封装

### 第 18 天 新版 Knife4j 接口文档集成

1. 引入最新 Knife4j 依赖
2. 全局文档配置、分组配置、接口注释规范
3. 实体类、DTO 参数文档注释编写
4. 前后端联调接口文档快速调试

### 第 19 天 登录体系全套集成（正式进入登录认证）

1. 新版密码加密工具整合（BCrypt 不可逆加密）
2. JWT 令牌生成、解析、过期配置
3. 全局登录拦截器新版实现
4. 登录状态校验、未登录拦截、放行白名单配置

### 第 20 天 脚手架最终定型 + RBAC 基础权限 + 验证码

1. 图形验证码功能开发（登录防刷）

2. RBAC 基础权限架构落地（用户 - 角色 - 菜单）

3. 简易接口权限控制雏形实现

4. 全套脚手架整合完毕

   

   阶段目标达成：15 分钟复制脚手架快速新建后端项目

------

# 第三阶段 第 21~32 天 两大企业实战项目（求职核心）

## 第 21~26 天 项目一：通用后台权限管理系统（职场刚需底层系统）

### 第 21 天 项目初始化 + 数据库全套权限表搭建

### 第 22 天 用户模块全套业务：分页查询、新增、编辑、禁用、逻辑删除

### 第 23 天 角色模块开发：角色新增、编辑、角色列表查询

### 第 24 天 用户分配角色、角色批量绑定用户业务

### 第 25 天 菜单管理开发、树形结构菜单查询、角色分配菜单权限

### 第 26 天 完整权限落地：登录认证、菜单动态渲染、按钮级权限控制、操作日志、文件上传下载、Excel 导入导出、事务管控、全局参数校验

## 第 27~32 天 项目二：电商后端业务系统（拉高简历含金量）

### 第 27 天 电商基础模块：商品分类、商品管理上下架

### 第 28 天 商品属性、规格参数、商品搜索业务实现

### 第 29 天 购物车完整业务逻辑开发

### 第 30 天 下单核心流程、订单创建、MySQL 事务强一致性保证

### 第 31 天 订单状态流转、确认收货、售后退货流程

### 第 32 天 库存管控、边界异常处理、项目整体优化复盘

------

# 第四阶段 第 33~36 天 主流中间件企业实战（面试高频）

## 第 33 天 Redis 全套企业实战

五大数据类型业务场景、登录令牌缓存、验证码缓存、热点商品缓存、接口限流、Redis 分布式锁解决超卖

## 第 34 天 RabbitMQ 消息队列实战

交换机 / 队列 / 消息收发、业务异步解耦、下单异步通知、死信队列实战

## 第 35 天 定时任务实战

Spring Task 原生定时任务、订单超时关闭实战

## 第 36 天 XXL-Job 分布式定时任务企业级整合

分布式任务调度、数据定时统计、定时同步业务

------

# 第五阶段 第 37~40 天 微服务架构 + 线上部署（入职加分）

## 第 37 天 Spring Cloud Alibaba 全套核心组件

Nacos 注册中心 + 配置中心、OpenFeign 远程调用、Sentinel 熔断限流

## 第 38 天 Gateway 网关统一路由、全局权限拦截、网关统一处理

## 第 39 天 单体电商项目拆分为微服务三大模块

用户服务、商品服务、订单服务完整拆分打通

## 第 40 天 工程化规范 + 线上项目部署

Git 团队协作规范、Linux 常用运维命令、SpringBoot 打包部署、Docker 容器化部署、线上日志排查、慢 SQL 优化、项目上线全流程