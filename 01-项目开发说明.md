# 项目开发说明

这份文档记录当前项目的开发约定和排错经验，配合 `00-数据库表结构文档.md` 使用。

## 1. 当前架构主线

项目现在走的是比较标准的前后端分离后台路线：

```text
Controller
  -> Service
  -> Mapper
  -> MySQL
```

安全链路由 Spring Security 接管：

```text
POST /sys/login
  -> LoginService 校验账号密码
  -> JwtUtil 生成 token
  -> 前端保存 token
  -> 请求业务接口时携带 Authorization: Bearer token
  -> JwtAuthenticationFilter 解析 token
  -> UserDetailsServiceImpl 查询用户权限
  -> SecurityContext 保存 Authentication
  -> @PreAuthorize 校验 permission_key
```

## 2. RBAC 权限模型

数据库使用五张表：

```text
sys_user
sys_role
sys_user_role
sys_permission
sys_role_permission
```

权限查询 SQL 的目标是取出当前用户拥有的 `permission_key`：

```sql
SELECT DISTINCT p.permission_key
FROM sys_permission p
INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
WHERE ur.user_id = #{userId}
  AND p.delete_flag = 0
  AND p.status = 1
  AND p.permission_key IS NOT NULL
  AND p.permission_key != '';
```

这些 `permission_key` 会被包装成 `SimpleGrantedAuthority`，供 `@PreAuthorize` 判断。

当前用户管理接口使用的权限：

| 权限标识 | 用途 |
| --- | --- |
| `sys:user:list` | 查询用户分页列表 |
| `sys:user:detail` | 查询用户详情 |
| `sys:user:status` | 启用 / 禁用用户 |
| `sys:user:resetPwd` | 重置用户密码 |
| `sys:user:add` | 新增用户 |
| `sys:user:edit` | 修改用户 |
| `sys:user:remove` | 逻辑删除用户 |
| `sys:user:physicalDel` | 管理员物理删除 |
| `sys:user:assignRole` | 查询 / 分配用户角色 |

当前用户修改自己的密码只要求登录，不额外配置按钮权限：

```text
PUT /sys/user/change-password
```

当前角色管理接口使用的权限：

| 权限标识 | 用途 |
| --- | --- |
| `sys:role:list` | 查询角色分页列表 |
| `sys:role:add` | 新增角色 |
| `sys:role:edit` | 修改角色 |
| `sys:role:status` | 启用 / 禁用角色 |
| `sys:role:remove` | 删除角色 |
| `sys:role:assignPermission` | 查询 / 分配角色权限 |

当前菜单权限管理接口使用的权限：

| 权限标识 | 用途 |
| --- | --- |
| `sys:menu:list` | 查询菜单权限树 |
| `sys:menu:add` | 新增菜单权限 |
| `sys:menu:edit` | 修改菜单权限 |
| `sys:menu:remove` | 删除菜单权限 |

角色可以绑定目录、菜单、按钮。登录鉴权时只会读取启用且 `permission_key` 非空的权限标识，避免目录 / 菜单空权限影响认证。

## 3. 登录与密码

登录入口：

```text
POST /sys/login
```

请求体：

```json
{
  "username": "admin",
  "password": "123456"
}
```

数据库中的 `password` 必须是 BCrypt 密文。不能存明文。

生成密文：

```bash
./mvnw -Dtest=PasswordEncoderTests test
```

生成指定明文：

```bash
./mvnw -Dtest=PasswordEncoderTests -DrawPassword=123456 test
```

如果登录失败并提示“账号或密码错误”，优先检查：

```sql
SELECT username, password, LENGTH(password), status
FROM sys_user
WHERE username = 'admin';
```

重点看：

- `password` 是否为 BCrypt 密文
- 密文长度是否完整
- `status` 是否为 1
- 项目连接的是否是你更新过的数据库

## 4. DTO / VO / Entity 约定

不要用一个对象承担所有场景。当前建议：

| 类型 | 用途 |
| --- | --- |
| `SysUser` | 数据库实体 |
| `LoginDTO` | 登录入参 |
| `UserAddDTO` | 新增用户入参，包含 password |
| `UserUpdateDTO` | 修改用户入参，不包含 password |
| `UserSearchDTO` | 查询条件 |
| `UserPageVO` | 查询结果返回，不包含 password |

核心原则：

- Controller 入参用 DTO
- Controller 出参用 VO
- Mapper 操作用 Entity
- `password` 只应该出现在登录、新增、重置密码等写入场景
- 查询返回不要暴露 `password/deleteFlag/version` 等内部字段

## 5. 新增用户流程

新增用户推荐链路：

```text
POST /sys/user/add
  -> UserAddDTO
  -> UserConvert 转 SysUser
  -> Service 校验 password
  -> PasswordEncoder.encode(password)
  -> sysUserMapper.insert(user)
```

新增接口需要权限：

```text
sys:user:add
```

新增时如果 `password` 为空，应该业务报错。不要在后端静默设置默认明文密码。

## 6. 修改用户流程

修改用户推荐链路：

```text
POST /sys/user/modify
  -> UserUpdateDTO
  -> 根据 id 查询 dbUser
  -> MapStruct 忽略 null 字段合并
  -> updateById
```

修改接口需要权限：

```text
sys:user:edit
```

普通用户资料修改不建议直接包含 `password`。密码修改应该独立成“修改密码/重置密码”接口。

## 7. 接口测试顺序

建议按这个顺序测试，方便定位问题：

1. 确认数据库连接正常。
2. 确认 admin 密码是 BCrypt 密文。
3. `POST /sys/login` 获取 token。
4. 不带 token 请求 `/sys/user/search_list`，预期 401。
5. 带 token 请求 `/sys/user/search_list`，admin 有 `sys:user:list` 时预期 200。
6. 去掉某个权限关系，再请求对应接口，预期 403。
7. 测新增、修改、删除。

请求业务接口时，token 头固定为：

```text
Authorization: Bearer <token>
```

## 8. 构建与测试

只看代码是否能编译：

```bash
./mvnw clean compile
```

跳过测试打包：

```bash
./mvnw clean package -DskipTests
```

完整构建：

```bash
./mvnw clean package
```

当前环境如果完整构建在 test 阶段出现 Mockito / ByteBuddy agent 报错，含义是测试运行环境不允许 Mockito inline mock maker 自挂载。这不是业务代码编译错误。后续可以单独处理测试插件或 Mockito 配置。

## 9. 常见排错

### 401 未登录

检查请求头：

```text
Authorization: Bearer <token>
```

不要再使用旧的自定义请求头：

```text
token: <token>
```

### 403 没权限

说明 token 有效，但权限不足。检查：

```sql
SELECT DISTINCT p.permission_key
FROM sys_permission p
INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
WHERE ur.user_id = 1
  AND p.delete_flag = 0
  AND p.status = 1
  AND p.permission_key IS NOT NULL
  AND p.permission_key != '';
```

返回值要能覆盖接口上的 `@PreAuthorize`。

### 登录成功但访问接口仍然 401

检查 `JwtAuthenticationFilter` 是否读取的是：

```text
Authorization
```

并且 token 是否带有：

```text
Bearer 
```

注意 `Bearer` 后面有一个空格。

### 权限 SQL 绑定失败

检查 `SysPermissionMapper.xml`：

```xml
<mapper namespace="com.system.mapper.SysPermissionMapper">
```

namespace 必须和 Java Mapper 接口全限定名一致。

### MapStruct warning

类似：

```text
Unmapped target properties
```

通常是 DTO/VO 和 Entity 字段不完全一致导致的提醒。它不一定是错误，但应该判断每个字段是否有意忽略。例如 `password` 不出现在 `UserPageVO` 中就是合理的。

## 10. 后续建议

- 为 `UserAddDTO`、`UserUpdateDTO` 增加 Validation 注解。
- 增加重置密码接口，单独处理密码变更。
- 增加角色管理、权限管理接口。
- 将普通用户详情接口也改为返回 VO。
- 完善单元测试，减少对真实数据库和完整 Spring 容器的依赖。
- 统一业务错误码，区分业务失败、未登录、无权限、参数错误。
