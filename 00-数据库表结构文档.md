# 数据库表结构文档

## 一、表结构概览

本系统采用经典的 RBAC（基于角色的访问控制）权限模型，共涉及以下五张核心表：

text

```
sys_user (用户表)
    ↓ (user_id → id)
sys_user_role (用户-角色关联表)
    ↓ (role_id → id)
sys_role (角色表)
    ↓ (role_id → id)
sys_role_permission (角色-权限关联表)
    ↓ (permission_id → id)
sys_permission (权限/菜单表)
```

通用能力表：

text

```
sys_operation_log (操作日志表)
sys_file (文件表)
```



### 权限链路图

text

```
当前登录用户
    ↓
sys_user_role（查询用户拥有哪些角色）
    ↓
sys_role（获取角色信息）
    ↓
sys_role_permission（查询角色拥有哪些权限）
    ↓
sys_permission（获取权限标识 permission_key）
    ↓
生成 Authentication.authorities（Spring Security 权限集合）
```



------

## 二、各表结构详情

### 1. sys_user（系统用户表）

#### DDL 建表语句

sql

```mysql
CREATE TABLE `sys_user` (
  `id` bigint NOT NULL COMMENT '主键',
  `username` varchar(30) NOT NULL COMMENT '登录账号',
  `password` varchar(100) NOT NULL COMMENT '登录密码',
  `nickname` varchar(50) NOT NULL COMMENT '用户昵称',
  `phone` varchar(11) DEFAULT '' COMMENT '手机号',
  `email` varchar(50) DEFAULT '' COMMENT '邮箱',
  `status` tinyint DEFAULT '1' COMMENT '状态 0禁用 1正常',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
  `delete_flag` tinyint DEFAULT '0' COMMENT '逻辑删除 0未删 1已删',
  `version` int DEFAULT '1' COMMENT '乐观锁版本号',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='系统用户表';
```



#### 字段说明

| 字段名      | 类型         | 约束                                | 说明                 |
| :---------- | :----------- | :---------------------------------- | :------------------- |
| id          | bigint       | PRIMARY KEY                         | 主键（雪花ID）       |
| username    | varchar(30)  | NOT NULL, UNIQUE                    | 登录账号             |
| password    | varchar(100) | NOT NULL                            | 登录密码             |
| nickname    | varchar(50)  | NOT NULL                            | 用户昵称             |
| phone       | varchar(11)  | DEFAULT ''                          | 手机号               |
| email       | varchar(50)  | DEFAULT ''                          | 邮箱                 |
| status      | tinyint      | DEFAULT 1                           | 状态 0禁用 1正常     |
| create_time | datetime     | DEFAULT CURRENT_TIMESTAMP           | 创建时间             |
| update_time | datetime     | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间             |
| delete_flag | tinyint      | DEFAULT 0                           | 逻辑删除 0未删 1已删 |
| version     | int          | DEFAULT 1                           | 乐观锁版本号         |

#### 当前数据

sql

```
-- 查询所有用户
SELECT * FROM sys_user;
```



| id                  | username | password | nickname    | phone       | email             | status | create_time         | update_time         | delete_flag | version |
| :------------------ | :------- | :------- | :---------- | :---------- | :---------------- | :----- | :------------------ | :------------------ | :---------- | :------ |
| 1                   | admin    | 123456   | Super管理员 | 18510044400 | niu_zilin@163.com | 1      | 2026-07-21 10:36:01 | 2026-07-21 13:22:27 | 0           | 1       |
| 2                   | test     | 123456   | 测试用户    | 18932550885 |                   | 1      | 2026-07-21 10:36:01 | 2026-07-21 14:20:12 | 0           | 1       |
| 2080122641338871810 | 佟麟     | 123456   | tonglin     | 13800138000 | tonglin@qq.com    | 1      | 2026-07-23 10:47:39 | 2026-07-23 10:47:39 | 0           | 1       |
| 2080224693553496065 | 你好北京 | 123456   | beijing     | 13800002345 | biywa@qq.com      | 1      | 2026-07-23 17:33:10 | 2026-07-23 17:33:10 | 0           | 3       |

------

### 2. sys_role（角色表）

#### DDL 建表语句

sql

```mysql
CREATE TABLE `sys_role` (
  `id` bigint NOT NULL COMMENT '主键',
  `role_name` varchar(50) NOT NULL COMMENT '角色名称',
  `role_code` varchar(50) NOT NULL COMMENT '角色标识',
  `sort` int DEFAULT '0' COMMENT '排序',
  `status` tinyint DEFAULT '1' COMMENT '状态 1启用 0禁用',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `delete_flag` tinyint DEFAULT '0',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_code` (`role_code`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='角色表';
```



#### 字段说明

| 字段名      | 类型        | 约束                                | 说明     |
| :---------- | :---------- | :---------------------------------- | :------- |
| id          | bigint      | PRIMARY KEY                         | 主键     |
| role_name   | varchar(50) | NOT NULL                            | 角色名称 |
| role_code   | varchar(50) | NOT NULL, UNIQUE                    | 角色标识 |
| sort        | int         | DEFAULT 0                           | 排序     |
| status      | tinyint     | DEFAULT 1                           | 状态     |
| create_time | datetime    | DEFAULT CURRENT_TIMESTAMP           | 创建时间 |
| update_time | datetime    | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间 |
| delete_flag | tinyint     | DEFAULT 0                           | 逻辑删除 |

#### 当前数据

sql

```
-- 查询所有角色
SELECT * FROM sys_role;
```



| id   | role_name    | role_code   | sort | status | create_time         | update_time         | delete_flag |
| :--- | :----------- | :---------- | :--- | :----- | :------------------ | :------------------ | :---------- |
| 1    | 超级管理员   | super_admin | 1    | 1      | 2026-07-21 10:20:24 | 2026-07-21 10:20:24 | 0           |
| 2    | 普通后台用户 | common_user | 2    | 1      | 2026-07-21 10:20:24 | 2026-07-21 10:20:24 | 0           |

------

### 3. sys_user_role（用户-角色关联表）

#### DDL 建表语句

sql

```mysql
CREATE TABLE `sys_user_role` (
  `id` bigint NOT NULL COMMENT '主键',
  `user_id` bigint NOT NULL COMMENT '用户ID',
  `role_id` bigint NOT NULL COMMENT '角色ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_role` (`user_id`,`role_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='用户角色关联表';
```



#### 字段说明

| 字段名      | 类型     | 约束                      | 说明                       |
| :---------- | :------- | :------------------------ | :------------------------- |
| id          | bigint   | PRIMARY KEY               | 主键                       |
| user_id     | bigint   | NOT NULL                  | 用户ID（关联 sys_user.id） |
| role_id     | bigint   | NOT NULL                  | 角色ID（关联 sys_role.id） |
| create_time | datetime | DEFAULT CURRENT_TIMESTAMP | 创建时间                   |

**唯一约束：** `uk_user_role` (user_id, role_id)

#### 当前数据

sql

```
-- 查询所有用户-角色关联
SELECT * FROM sys_user_role;
```



| id   | user_id | role_id | create_time         |
| :--- | :------ | :------ | :------------------ |
| 1    | 1       | 1       | 2026-07-28 18:16:58 |

------

### 4. sys_permission（菜单/权限表）

#### DDL 建表语句

sql

```mysql
CREATE TABLE `sys_permission` (
  `id` bigint NOT NULL COMMENT '主键',
  `parent_id` bigint DEFAULT '0' COMMENT '父菜单ID',
  `name` varchar(50) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci NOT NULL COMMENT '菜单名称',
  `menu_path` varchar(255) DEFAULT '' COMMENT '路由地址',
  `permission_key` varchar(255) CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci DEFAULT '' COMMENT '权限标识',
  `type` tinyint DEFAULT NULL COMMENT '类型 1目录 2菜单 3按钮',
  `sort` int DEFAULT '0',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  `update_time` datetime DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  `delete_flag` tinyint DEFAULT '0',
  `icon` varchar(100) DEFAULT '' COMMENT '菜单图标',
  `component` varchar(255) DEFAULT '' COMMENT '前端组件路径',
  `visible` tinyint DEFAULT '1' COMMENT '是否显示(1显示 0隐藏)',
  `status` tinyint DEFAULT '1' COMMENT '状态(1启用 0禁用)',
  `redirect` varchar(255) DEFAULT '' COMMENT '路由重定向',
  `is_cache` tinyint DEFAULT '1' COMMENT '是否缓存(1缓存 0不缓存)',
  `is_frame` tinyint DEFAULT '0' COMMENT '是否外链(0否 1是)',
  `remark` varchar(500) DEFAULT '' COMMENT '备注',
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='菜单权限表';
```



#### 字段说明

| 字段名         | 类型         | 约束                                | 说明                   |
| :------------- | :----------- | :---------------------------------- | :--------------------- |
| id             | bigint       | PRIMARY KEY                         | 主键                   |
| parent_id      | bigint       | DEFAULT 0                           | 父菜单ID               |
| name           | varchar(50)  | NOT NULL                            | 菜单名称               |
| menu_path      | varchar(255) | DEFAULT ''                          | 路由地址               |
| permission_key | varchar(255) | DEFAULT ''                          | 权限标识               |
| type           | tinyint      |                                     | 类型 1目录 2菜单 3按钮 |
| sort           | int          | DEFAULT 0                           | 排序                   |
| create_time    | datetime     | DEFAULT CURRENT_TIMESTAMP           | 创建时间               |
| update_time    | datetime     | DEFAULT CURRENT_TIMESTAMP ON UPDATE | 更新时间               |
| delete_flag    | tinyint      | DEFAULT 0                           | 逻辑删除               |
| icon           | varchar(100) | DEFAULT ''                          | 菜单图标               |
| component      | varchar(255) | DEFAULT ''                          | 前端组件路径           |
| visible        | tinyint      | DEFAULT 1                           | 是否显示 1显示 0隐藏   |
| status         | tinyint      | DEFAULT 1                           | 状态 1启用 0禁用       |
| redirect       | varchar(255) | DEFAULT ''                          | 路由重定向             |
| is_cache       | tinyint      | DEFAULT 1                           | 是否缓存 1缓存 0不缓存 |
| is_frame       | tinyint      | DEFAULT 0                           | 是否外链 0否 1是       |
| remark         | varchar(500) | DEFAULT ''                          | 备注                   |

#### 当前数据

sql

```
-- 查询所有权限（按树形结构排序）
SELECT * FROM sys_permission ORDER BY parent_id, sort;
```



| id | parent_id | name | menu_path | permission_key | type | sort | create_time | update_time | delete_flag | icon | component | visible | status | redirect | is_cache | is_frame | remark |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | 0 | 系统管理 | /system | | 1 | 1 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | Setting | Layout | 1 | 1 | /system/user | 1 | 0 | 系统管理目录 |
| 2 | 1 | 用户管理 | /system/user | | 2 | 1 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | User | system/user/index | 1 | 1 | | 1 | 0 | 用户管理菜单 |
| 3 | 2 | 用户列表 | | sys:user:list | 3 | 1 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | | | 1 | 1 | | 1 | 0 | 查询用户权限 |
| 4 | 2 | 新增用户 | | sys:user:add | 3 | 2 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | | | 1 | 1 | | 1 | 0 | 新增用户权限 |
| 5 | 2 | 修改用户 | | sys:user:edit | 3 | 3 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | | | 1 | 1 | | 1 | 0 | 修改用户权限 |
| 6 | 2 | 删除用户 | | sys:user:remove | 3 | 4 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | | | 1 | 1 | | 1 | 0 | 逻辑删除用户权限 |
| 7 | 2 | 物理删除 | | sys:user:physicalDel | 3 | 5 | 2026-07-28 17:46:43 | 2026-07-28 17:46:43 | 0 | | | 1 | 1 | | 1 | 0 | 管理员专用物理删除权限 |
| 8 | 2 | 用户详情 | | sys:user:detail | 3 | 6 | 2026-07-29 15:55:00 | 2026-07-29 15:55:00 | 0 | | | 1 | 1 | | 1 | 0 | 查询用户详情权限 |
| 9 | 2 | 修改状态 | | sys:user:status | 3 | 7 | 2026-07-29 16:10:00 | 2026-07-29 16:10:00 | 0 | | | 1 | 1 | | 1 | 0 | 启用禁用用户权限 |
| 10 | 2 | 重置密码 | | sys:user:resetPwd | 3 | 8 | 2026-07-29 16:45:00 | 2026-07-29 16:45:00 | 0 | | | 1 | 1 | | 1 | 0 | 重置用户密码权限 |
| 17 | 2 | 分配角色 | | sys:user:assignRole | 3 | 9 | 2026-07-30 10:30:00 | 2026-07-30 10:30:00 | 0 | | | 1 | 1 | | 1 | 0 | 查询和分配用户角色权限 |

如果数据库还没有新增权限，需要先插入权限记录：

```sql
INSERT INTO sys_permission
(id, parent_id, name, menu_path, permission_key, type, sort, delete_flag, visible, status, is_cache, is_frame, remark)
VALUES
(8, 2, '用户详情', '', 'sys:user:detail', 3, 6, 0, 1, 1, 1, 0, '查询用户详情权限'),
(9, 2, '修改状态', '', 'sys:user:status', 3, 7, 0, 1, 1, 1, 0, '启用禁用用户权限'),
(10, 2, '重置密码', '', 'sys:user:resetPwd', 3, 8, 0, 1, 1, 1, 0, '重置用户密码权限'),
(17, 2, '分配角色', '', 'sys:user:assignRole', 3, 9, 0, 1, 1, 1, 0, '查询和分配用户角色权限');
```

新增用户详情、修改状态、重置密码、分配角色权限后，需要给管理员角色补关联。`sys_role_permission.id` 请使用不冲突的雪花 ID 或当前表中不存在的主键：

```sql
INSERT INTO sys_role_permission (id, role_id, permission_id)
VALUES
(这里替换为不冲突的主键ID, 1, 8),
(这里替换为另一个不冲突的主键ID, 1, 9),
(这里替换为另一个不冲突的主键ID, 1, 10),
(这里替换为另一个不冲突的主键ID, 1, 17);
```

------

### 5. sys_role_permission（角色-权限关联表）

#### DDL 建表语句

sql

```
CREATE TABLE `sys_role_permission` (
  `id` bigint NOT NULL COMMENT '主键',
  `role_id` bigint NOT NULL,
  `permission_id` bigint NOT NULL,
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_role_menu` (`role_id`,`permission_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='角色菜单关联表';
```



#### 字段说明

| 字段名        | 类型     | 约束                      | 说明                             |
| :------------ | :------- | :------------------------ | :------------------------------- |
| id            | bigint   | PRIMARY KEY               | 主键                             |
| role_id       | bigint   | NOT NULL                  | 角色ID（关联 sys_role.id）       |
| permission_id | bigint   | NOT NULL                  | 权限ID（关联 sys_permission.id） |
| create_time   | datetime | DEFAULT CURRENT_TIMESTAMP | 创建时间                         |

**唯一约束：** `uk_role_menu` (role_id, permission_id)

#### 当前数据

sql

```
-- 查询所有角色-权限关联
SELECT * FROM sys_role_permission;
```



| id   | role_id | permission_id | create_time         |
| :--- | :------ | :------------ | :------------------ |
| 1    | 1       | 3             | 2026-07-29 09:19:38 |
| 2    | 1       | 4             | 2026-07-29 09:19:38 |
| 3    | 1       | 5             | 2026-07-29 09:19:38 |
| 4    | 1       | 6             | 2026-07-29 09:19:38 |
| 5    | 1       | 7             | 2026-07-29 09:19:38 |

------

### 6. sys_operation_log（操作日志表）

#### DDL 建表语句

sql

```mysql
CREATE TABLE `sys_operation_log` (
  `id` bigint NOT NULL COMMENT '主键',
  `module` varchar(100) DEFAULT '' COMMENT '业务模块',
  `operation` varchar(100) DEFAULT '' COMMENT '操作名称',
  `request_method` varchar(20) DEFAULT '' COMMENT '请求方式',
  `request_uri` varchar(255) DEFAULT '' COMMENT '请求地址',
  `request_params` text COMMENT '请求参数',
  `response_result` text COMMENT '响应结果',
  `status` tinyint DEFAULT '1' COMMENT '状态 1成功 0失败',
  `error_msg` varchar(2000) DEFAULT '' COMMENT '错误信息',
  `operator_id` bigint DEFAULT NULL COMMENT '操作人ID',
  `ip` varchar(64) DEFAULT '' COMMENT '请求IP',
  `cost_time` bigint DEFAULT '0' COMMENT '耗时毫秒',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_operator_id` (`operator_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='操作日志表';
```

#### 字段说明

| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | bigint | 主键，雪花ID |
| module | varchar(100) | 业务模块 |
| operation | varchar(100) | 操作名称 |
| request_method | varchar(20) | 请求方式 |
| request_uri | varchar(255) | 请求地址 |
| request_params | text | 请求参数，敏感字段脱敏 |
| response_result | text | 响应结果 |
| status | tinyint | 1成功 0失败 |
| error_msg | varchar(2000) | 异常信息 |
| operator_id | bigint | 操作人ID |
| ip | varchar(64) | 请求IP |
| cost_time | bigint | 耗时毫秒 |
| create_time | datetime | 创建时间 |

------

### 7. sys_file（文件表）

#### DDL 建表语句

sql

```mysql
CREATE TABLE `sys_file` (
  `id` bigint NOT NULL COMMENT '主键',
  `original_name` varchar(255) NOT NULL COMMENT '原始文件名',
  `storage_name` varchar(255) NOT NULL COMMENT '存储文件名',
  `relative_path` varchar(500) NOT NULL COMMENT '相对存储路径',
  `content_type` varchar(100) DEFAULT '' COMMENT '文件类型',
  `file_size` bigint DEFAULT '0' COMMENT '文件大小，单位字节',
  `uploader_id` bigint DEFAULT NULL COMMENT '上传人ID',
  `create_time` datetime DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
  PRIMARY KEY (`id`),
  KEY `idx_uploader_id` (`uploader_id`),
  KEY `idx_create_time` (`create_time`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_0900_ai_ci COMMENT='文件表';
```

#### 字段说明

| 字段名 | 类型 | 说明 |
| :--- | :--- | :--- |
| id | bigint | 主键，雪花ID |
| original_name | varchar(255) | 原始文件名 |
| storage_name | varchar(255) | 实际存储文件名 |
| relative_path | varchar(500) | 相对存储路径 |
| content_type | varchar(100) | 文件类型 |
| file_size | bigint | 文件大小，单位字节 |
| uploader_id | bigint | 上传人ID |
| create_time | datetime | 创建时间 |

------

## 三、核心查询 SQL

### 3.1 查询用户拥有的所有权限标识

sql

```
SELECT DISTINCT p.permission_key
FROM sys_permission p
INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
WHERE ur.user_id = #{userId}
  AND p.delete_flag = 0
  AND p.status = 1
  AND p.permission_key IS NOT NULL
  AND p.permission_key != '';
```



### 3.2 查询用户拥有的所有角色

sql

```
SELECT r.*
FROM sys_role r
INNER JOIN sys_user_role ur ON r.id = ur.role_id
WHERE ur.user_id = #{userId}
  AND r.delete_flag = 0;
```



### 3.3 查询角色拥有的所有权限

sql

```
SELECT p.*
FROM sys_permission p
INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
WHERE rp.role_id = #{roleId}
  AND p.delete_flag = 0
ORDER BY p.parent_id, p.sort;
```

### 3.4 查询当前用户可见菜单

sql

```
SELECT DISTINCT p.*
FROM sys_permission p
INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
INNER JOIN sys_role r ON ur.role_id = r.id
WHERE ur.user_id = #{userId}
  AND p.delete_flag = 0
  AND p.status = 1
  AND p.visible = 1
  AND p.type IN (1, 2)
  AND r.delete_flag = 0
  AND r.status = 1
ORDER BY p.parent_id, p.sort, p.id;
```

当前用户菜单树依赖角色同时绑定父级目录 / 菜单。例如要展示“系统管理 -> 用户管理”，角色需要绑定 `sys_permission.id = 1` 和 `2`。



### 3.5 针对 admin 用户（id=1）的权限查询结果

sql

```mysql
SELECT DISTINCT p.permission_key
FROM sys_permission p
INNER JOIN sys_role_permission rp ON p.id = rp.permission_id
INNER JOIN sys_user_role ur ON rp.role_id = ur.role_id
WHERE ur.user_id = 1 
  AND p.delete_flag = 0
  AND p.status = 1
  AND p.permission_key IS NOT NULL
  AND p.permission_key != '';
```



**返回结果：**

text

```
["sys:user:list", "sys:user:detail", "sys:user:status", "sys:user:resetPwd", "sys:user:add", "sys:user:edit", "sys:user:remove", "sys:user:physicalDel", "sys:user:assignRole", "sys:role:list", "sys:role:add", "sys:role:edit", "sys:role:status", "sys:role:remove", "sys:role:assignPermission", "sys:menu:list", "sys:menu:add", "sys:menu:edit", "sys:menu:remove"]
```

角色管理接口需要补充按钮权限，建议挂在系统管理目录下。`id` 可以按你当前库中不冲突的主键调整：

```sql
INSERT INTO sys_permission
(id, parent_id, name, menu_path, permission_key, type, sort, delete_flag, visible, status, is_cache, is_frame, remark)
VALUES
(11, 1, '角色管理', '/system/role', '', 2, 2, 0, 1, 1, 1, 0, '角色管理菜单'),
(12, 11, '角色列表', '', 'sys:role:list', 3, 1, 0, 1, 1, 1, 0, '查询角色权限'),
(13, 11, '新增角色', '', 'sys:role:add', 3, 2, 0, 1, 1, 1, 0, '新增角色权限'),
(14, 11, '修改角色', '', 'sys:role:edit', 3, 3, 0, 1, 1, 1, 0, '修改角色权限'),
(15, 11, '删除角色', '', 'sys:role:remove', 3, 4, 0, 1, 1, 1, 0, '删除角色权限'),
(16, 11, '修改状态', '', 'sys:role:status', 3, 5, 0, 1, 1, 1, 0, '启用禁用角色权限'),
(23, 11, '分配权限', '', 'sys:role:assignPermission', 3, 6, 0, 1, 1, 1, 0, '查询和分配角色权限');
```

给管理员角色补关联时，`sys_role_permission.id` 继续使用不冲突主键：

```sql
INSERT INTO sys_role_permission (id, role_id, permission_id)
VALUES
(这里替换为不冲突的主键ID, 1, 12),
(这里替换为另一个不冲突的主键ID, 1, 13),
(这里替换为另一个不冲突的主键ID, 1, 14),
(这里替换为另一个不冲突的主键ID, 1, 15),
(这里替换为另一个不冲突的主键ID, 1, 16),
(这里替换为另一个不冲突的主键ID, 1, 23);
```

菜单权限管理接口需要补充按钮权限，建议同样挂在系统管理目录下。`id` 可以按你当前库中不冲突的主键调整：

```sql
INSERT INTO sys_permission
(id, parent_id, name, menu_path, permission_key, type, sort, delete_flag, visible, status, is_cache, is_frame, remark)
VALUES
(18, 1, '菜单管理', '/system/menu', '', 2, 3, 0, 1, 1, 1, 0, '菜单权限管理菜单'),
(19, 18, '菜单列表', '', 'sys:menu:list', 3, 1, 0, 1, 1, 1, 0, '查询菜单权限树'),
(20, 18, '新增菜单', '', 'sys:menu:add', 3, 2, 0, 1, 1, 1, 0, '新增菜单权限'),
(21, 18, '修改菜单', '', 'sys:menu:edit', 3, 3, 0, 1, 1, 1, 0, '修改菜单权限'),
(22, 18, '删除菜单', '', 'sys:menu:remove', 3, 4, 0, 1, 1, 1, 0, '删除菜单权限');
```

给管理员角色补关联时，`sys_role_permission.id` 继续使用不冲突主键：

```sql
INSERT INTO sys_role_permission (id, role_id, permission_id)
VALUES
(这里替换为不冲突的主键ID, 1, 19),
(这里替换为另一个不冲突的主键ID, 1, 20),
(这里替换为另一个不冲突的主键ID, 1, 21),
(这里替换为另一个不冲突的主键ID, 1, 22);
```



------

## 四、初始化数据 SQL

### 4.1 插入角色数据

sql

```
INSERT INTO sys_role (id, role_name, role_code, sort, delete_flag)
VALUES 
(1, '超级管理员', 'super_admin', 1, 0),
(2, '普通后台用户', 'common_user', 2, 0);
```



### 4.2 插入用户-角色关联数据

sql

```
INSERT INTO sys_user_role (id, user_id, role_id)
VALUES (1, 1, 1);
```



### 4.3 插入角色-权限关联数据

sql

```
INSERT INTO sys_role_permission (id, role_id, permission_id, create_time)
VALUES 
(1, 1, 3, NOW()),
(2, 1, 4, NOW()),
(3, 1, 5, NOW()),
(4, 1, 6, NOW()),
(5, 1, 7, NOW());
```



------

## 五、表关系图

text

```
┌─────────────────────┐          ┌──────────────────────┐          ┌─────────────────────┐
│     sys_user        │          │    sys_user_role     │          │      sys_role       │
├─────────────────────┤          ├──────────────────────┤          ├─────────────────────┤
│ id (PK)             │◄────────│ user_id (FK)         │          │ id (PK)             │
│ username (UK)       │          │ role_id (FK)         │─────────►│ role_name           │
│ password            │          │ create_time          │          │ role_code (UK)      │
│ nickname            │          └──────────────────────┘          │ sort                │
│ phone               │                                            │ delete_flag         │
│ email               │                                            └─────────────────────┘
│ status              │                                                     │
│ create_time         │                                                     │
│ update_time         │                                                     │
│ delete_flag         │          ┌──────────────────────┐                    │
│ version             │          │ sys_role_permission  │                    │
└─────────────────────┘          ├──────────────────────┤                    │
                                  │ id (PK)             │                    │
                                  │ permission_id (FK)  │                    │
┌─────────────────────┐          │ role_id (FK)        │────────────────────┘
│   sys_permission    │          │ create_time          │
├─────────────────────┤          └──────────────────────┘
│ id (PK)             │◄─────────┘
│ parent_id           │
│ name                │
│ menu_path           │
│ permission_key      │
│ type (1目录2菜单3按钮)│
│ sort                │
│ icon                │
│ component           │
│ visible             │
│ status              │
│ redirect            │
│ is_cache            │
│ is_frame            │
│ remark              │
│ delete_flag         │
│ create_time         │
│ update_time         │
└─────────────────────┘
```
