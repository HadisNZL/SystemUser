# 微服务部署运维手册

本文档记录当前项目的本地开发、Docker Compose 部署和基础排错命令。

## 一、运行单元

```text
gateway-service  9000  统一入口
auth-service     9101  登录认证
system-service   9201  系统管理
log-service      9301  操作日志
file-service     9401  文件管理
```

中间件：

```text
MySQL      3306
Redis      6379
RabbitMQ   5672 / 15672
Nacos      8848 / 9848 / 9849
```

根 `pom.xml` 是 Maven 聚合父工程，不再生成或启动 `admin-system` 单体 Jar。

## 二、本地开发

一键启动中间件和本地微服务：

```bash
./scripts/dev.sh start
./scripts/dev.sh status
./scripts/dev.sh logs
./scripts/dev.sh stop
```

脚本会先安装 `admin-common`、`admin-operation-log`，再依次启动各微服务。

本地模式与 Docker 应用模式使用相同端口，不能同时启动。切换前先停止另一种模式：

```bash
./scripts/dev.sh stop
./scripts/app.sh stop
```

单独启动服务前，先安装公共模块：

```bash
./mvnw -N install -DskipTests
./mvnw -pl admin-common,admin-operation-log install -DskipTests
./mvnw -f system-service/pom.xml spring-boot:run
```

其他服务替换对应 `pom.xml` 即可。

## 三、Docker Compose

只启动中间件：

```bash
./scripts/app.sh middleware
```

构建并启动全部服务：

```bash
./scripts/app.sh start
```

常用命令：

```bash
./scripts/app.sh build
./scripts/app.sh ps
./scripts/app.sh logs
./scripts/app.sh restart
./scripts/app.sh stop
```

也可以直接使用：

```bash
./mvnw clean package -DskipTests
docker compose up -d --build
docker compose ps
docker compose logs -f
```

不要执行 `docker compose down -v`，除非明确需要删除数据库和文件卷。

## 四、健康检查

```text
http://localhost:9000/actuator/health
http://localhost:9101/actuator/health
http://localhost:9201/actuator/health
http://localhost:9301/actuator/health
http://localhost:9401/actuator/health
```

正常响应：

```json
{"status":"UP"}
```

开发和 Docker 环境的统一接口文档：

```text
http://localhost:9000/doc.html
```

Knife4j 通过网关聚合以下 OpenAPI 文档：

```text
/v3/api-docs/auth
/v3/api-docs/system
/v3/api-docs/file
/v3/api-docs/log
```

页面左上角下拉框用于切换认证、系统、文件、日志服务；选中服务后，左侧按 `@Tag` 展示具体业务模块。

`prod` 环境默认关闭 Knife4j 和各服务的 OpenAPI JSON，避免接口信息公开。

Nacos 服务列表应包含：

```text
gateway-service
auth-service
system-service
log-service
file-service
```

## 五、日志排查

本地开发日志：

```text
logs/dev/gateway-service.log
logs/dev/auth-service.log
logs/dev/system-service.log
logs/dev/log-service.log
logs/dev/file-service.log
```

Docker 日志：

```bash
docker compose logs -f gateway-service
docker compose logs -f auth-service
docker compose logs -f system-service
docker compose logs -f log-service
docker compose logs -f file-service
```

## 六、数据与备份

Docker named volume：

```text
admin_mysql_data
admin_redis_data
admin_rabbitmq_data
admin_nacos_data
admin_app_upload
```

`admin_app_upload` 挂载到 `file-service:/app/upload`。

MySQL 备份：

```bash
docker exec admin-mysql mysqldump -uroot -p123456 \
  --single-transaction --set-gtid-purged=OFF admin_system \
  > docker/mysql/backup/admin_system.sql
```

恢复：

```bash
docker exec -i admin-mysql mysql -uroot -p123456 admin_system \
  < docker/mysql/backup/admin_system.sql
```

## 七、常见排错

端口占用：

```bash
lsof -iTCP:9000 -sTCP:LISTEN
lsof -iTCP:9401 -sTCP:LISTEN
```

服务未注册：

1. 检查 Nacos 是否启动。
2. 检查服务日志中的 Nacos 地址。
3. 检查 8848 和 9848 端口。

网关返回 503：

1. 检查目标服务是否启动。
2. 检查 Nacos 服务列表是否存在目标实例。
3. 检查网关路由前缀是否正确。

接口返回 401：

1. 检查 `Authorization: Bearer token`。
2. 检查 Token 是否过期或已退出。
3. 确认业务请求访问的是网关 `9000`，而不是业务服务端口。
4. Knife4j 的全局 Header 与 `bearerAuth` 授权不要同时配置。
5. 下游身份过滤器必须禁止 Servlet 自动注册，只保留 Spring Security 链内注册。

接口返回 403：

1. 检查 `sys_user_role`。
2. 检查 `sys_role_permission`。
3. 检查 `sys_permission.permission_key`。
4. 清理对应用户的 Redis 权限缓存。
