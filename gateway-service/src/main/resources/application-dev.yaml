spring:
  config:
    import:
      - optional:nacos:gateway-service-dev.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        group: DEFAULT_GROUP

logging:
  level:
    root: info
    com.system.gateway: debug
