# 企业级后端学习规划

这份规划基于当前 `admin-system` 项目的真实进度重新整理。原始 `JAVA后台详细学习.md` 的大方向可以保留，但当前项目已经提前进入 JWT、Spring Security、RBAC 阶段，因此后续路线应该从“补齐脚手架基础”和“完整权限管理系统”继续推进。

## 当前项目位置

当前项目已经具备：

- Spring Boot 3.5.16 基础工程
- MyBatis-Plus CRUD、分页、逻辑删除、自动填充、乐观锁
- 统一返回 `Result`
- 全局异常处理
- Knife4j Next 接口文档
- DTO / VO / Entity 初步分层
- BCrypt 密码加密
- JWT 登录
- Spring Security 无状态认证
- RBAC 五表模型
- `@PreAuthorize` 接口权限控制

所以不建议从原计划第 10 天重新走，也不建议马上跳到电商项目。现在最合适的主线是：

```text
补齐脚手架基础
  -> 完整后台权限管理系统
  -> 企业通用能力
  -> 中间件
  -> 部署工程化
  -> 微服务架构
  -> 网关统一认证
  -> 微服务部署与可观测性
```

## 阶段一：脚手架基础收口

目标：把当前项目从“功能能跑”整理成“后续可持续扩展”的工程底座。

### 1. 参数校验完善

完成内容：

- 给 `UserAddDTO` 增加校验注解
- 给 `UserUpdateDTO` 增加校验注解
- 给分页参数增加边界校验
- 全局处理 `MethodArgumentNotValidException`
- 全局处理 `ConstraintViolationException`

建议重点：

- 新增用户时 `username/password/nickname` 必填
- 手机号、邮箱格式校验
- 页码不能小于 1
- 每页条数设置合理上限

### 2. 统一错误码

完成内容：

- 抽象业务错误码枚举
- 区分参数错误、业务错误、未登录、无权限、系统错误
- 统一 `Result` 与 `SecurityResult` 的返回风格

建议错误码：

```text
200 操作成功
400 参数错误
401 未登录或 token 失效
403 没有权限
500 系统异常
```

### 3. DTO / VO / Entity 边界固化

完成内容：

- 新增接口只用 `UserAddDTO`
- 修改接口只用 `UserUpdateDTO`
- 查询接口只返回 `UserPageVO` 或详情 VO
- Entity 不直接作为主要接口出参
- 明确 MapStruct 忽略字段，消除不必要 warning

原则：

```text
入参 DTO
出参 VO
数据库 Entity
转换 Convert
```

### 4. 构建与测试整理

完成内容：

- 解决 `clean package` 中 Mockito / ByteBuddy agent 报错
- 保留 `PasswordEncoderTests` 作为开发期工具
- 增加最小可用的登录测试、权限测试

目标命令：

```bash
./mvnw clean compile
./mvnw clean package
```

都应该有明确、稳定的结果。

## 阶段二：用户模块闭环

目标：把用户管理从 CRUD 升级成后台系统的完整用户模块。

### 1. 用户详情

接口建议：

```text
GET /sys/user/{id}
```

权限建议：

```text
sys:user:detail
```

返回 `UserDetailVO`，不返回密码。

### 2. 启用 / 禁用用户

接口建议：

```text
PUT /sys/user/status
```

入参建议：

```json
{
  "id": 1,
  "status": 0
}
```

权限建议：

```text
sys:user:status
```

### 3. 重置密码

接口建议：

```text
PUT /sys/user/reset-password
```

权限建议：

```text
sys:user:resetPwd
```

说明：

- 管理员重置别人的密码
- 新密码必须 BCrypt 后入库
- 不返回密码

### 4. 修改当前用户密码

接口建议：

```text
PUT /sys/user/change-password
```

入参建议：

```json
{
  "oldPassword": "123456",
  "newPassword": "newPassword"
}
```

说明：

- 普通用户修改自己的密码
- 必须校验旧密码
- 修改后可考虑让旧 token 失效，后续接 Redis 时再增强

## 阶段三：角色模块

目标：完成 RBAC 中的角色管理。

### 1. 新增表实体与基础结构

新增：

```text
SysRole
SysRoleMapper
SysRoleService
SysRoleServiceImpl
SysRoleController
RoleAddDTO
RoleUpdateDTO
RoleSearchDTO
RolePageVO
```

### 2. 角色基础接口

接口建议：

```text
GET    /sys/role/list
GET    /sys/role/search_list
POST   /sys/role/add
POST   /sys/role/modify
DELETE /sys/role/delete/{id}
```

权限建议：

```text
sys:role:list
sys:role:add
sys:role:edit
sys:role:remove
```

### 3. 角色状态管理

接口建议：

```text
PUT /sys/role/status
```

权限建议：

```text
sys:role:status
```

## 阶段四：用户分配角色

目标：把 `sys_user_role` 用起来，让用户和角色可配置。

### 1. 查询用户已有角色

接口建议：

```text
GET /sys/user/{id}/roles
```

### 2. 给用户分配角色

接口建议：

```text
PUT /sys/user/{id}/roles
```

入参建议：

```json
{
  "roleIds": [1, 2]
}
```

实现建议：

- 先删除旧关系
- 再批量插入新关系
- 加事务
- 注意唯一索引冲突

权限建议：

```text
sys:user:assignRole
```

## 阶段五：菜单与权限模块

目标：让权限不再只是数据库静态数据，而是后台可维护。

### 1. 菜单树查询

接口建议：

```text
GET /sys/menu/tree
```

返回目录、菜单、按钮的树形结构。

### 2. 菜单新增、修改、删除

接口建议：

```text
POST   /sys/menu/add
POST   /sys/menu/modify
DELETE /sys/menu/delete/{id}
```

权限建议：

```text
sys:menu:list
sys:menu:add
sys:menu:edit
sys:menu:remove
```

### 3. 权限 key 规范

继续使用：

```text
模块:资源:动作
```

示例：

```text
sys:user:list
sys:user:add
sys:role:list
sys:menu:add
```

### 4. 菜单删除保护

删除菜单前检查：

- 是否存在子菜单
- 是否已被角色绑定

避免误删造成权限断裂。

## 阶段六：角色分配菜单权限

目标：完整打通 `sys_role_permission`。

### 1. 查询角色权限

接口建议：

```text
GET /sys/role/{id}/permissions
```

### 2. 给角色分配权限

接口建议：

```text
PUT /sys/role/{id}/permissions
```

入参建议：

```json
{
  "permissionIds": [3, 4, 5, 6, 7]
}
```

实现建议：

- 加事务
- 删除旧关系
- 批量插入新关系
- 注意权限为空时代表清空权限

权限建议：

```text
sys:role:assignPermission
```

## 阶段七：登录后用户信息与动态菜单

目标：贴近真实前后端分离后台。

### 1. 当前登录用户信息

接口建议：

```text
GET /sys/profile
```

返回：

```json
{
  "user": {},
  "roles": [],
  "permissions": []
}
```

### 2. 当前用户菜单树

接口建议：

```text
GET /sys/menu/current
```

说明：

- 根据当前用户角色查询可见菜单
- 只返回目录和菜单
- 按 `parent_id` 和 `sort` 组装树
- 按 `visible/status/delete_flag` 过滤

### 3. 按钮级权限

前端拿到 `permissions` 后控制按钮显示。后端仍然保留 `@PreAuthorize`，不能只依赖前端隐藏按钮。

## 阶段八：企业通用能力

目标：补齐后台项目常见加分项。

### 1. 验证码

登录接口增加验证码：

```text
GET  /captcha
POST /sys/login
```

初期可以用内存保存验证码，后续 Redis 阶段迁移到 Redis。

### 2. 操作日志

实现方向：

- 自定义注解 `@OperationLog`
- AOP 切面记录操作
- 记录操作人、接口、参数、结果、耗时、IP

### 3. 文件上传下载

实现方向：

- 本地文件存储
- 文件大小限制
- 文件类型校验
- 后续扩展对象存储

### 4. Excel 导入导出

实现方向：

- 用户列表导出
- 用户批量导入
- 导入失败原因收集

### 5. 字典配置

常见后台能力：

- 用户状态字典
- 菜单类型字典
- 是否启用字典

## 阶段九：缓存与中间件

目标：进入面试和企业开发高频区域。

### 1. Redis

优先场景：

- 登录用户信息缓存
- 权限缓存
- 验证码缓存
- token 黑名单
- 接口限流

### 2. RabbitMQ

优先场景：

- 操作日志异步写入
- 邮件或短信通知
- 业务解耦

### 3. 定时任务

先用 Spring Task：

- 清理过期日志
- 定时统计数据

后续再学 XXL-Job。

## 阶段十：单体项目部署与工程化

目标：让项目从本地练习变成可以上线运行。

### 1. 打包部署

掌握：

```bash
./mvnw clean package -DskipTests
java -jar target/admin-system-0.0.1-SNAPSHOT.jar
```

### 2. Linux 基础

重点：

- 查看进程
- 查看端口
- 查看日志
- 环境变量
- 后台运行

### 3. Docker

目标：

- Dockerfile
- docker build
- docker run
- docker compose 编排 MySQL + 后端服务

### 4. Git 工作流

掌握：

- feature 分支
- commit 规范
- pull request
- code review
- tag 与 release

## 阶段十一：微服务架构入门

目标：理解为什么要拆微服务，以及如何从当前单体后台平滑演进。

### 1. 微服务拆分原则

先不要为了“微服务”而微服务。拆分前必须理解：

- 单体项目的优势与边界
- 服务边界如何划分
- 数据库是否跟着拆
- 分布式事务为什么复杂
- 服务间调用失败如何处理
- 配置、日志、链路追踪如何统一

当前项目推荐的拆分练习方向：

```text
admin-system 单体
  -> system-service    用户、角色、菜单、权限
  -> auth-service      登录、token、验证码
  -> file-service      文件上传下载
  -> log-service       操作日志
```

初学阶段可以先“代码逻辑拆服务”，不要一开始就拆数据库。

### 2. Spring Cloud Alibaba 基础组件

学习内容：

- Nacos 注册中心
- Nacos 配置中心
- OpenFeign 服务调用
- Sentinel 限流熔断
- Spring Cloud Gateway 网关

建议顺序：

```text
Nacos 注册中心
  -> OpenFeign 服务调用
  -> Nacos 配置中心
  -> Gateway 统一入口
  -> Sentinel 限流熔断
```

### 3. 第一个微服务拆分目标

先从最小闭环开始：

```text
gateway-service
auth-service
system-service
```

请求链路：

```text
前端
  -> gateway-service
  -> auth-service 登录签发 token
  -> system-service 用户/角色/菜单接口
```

目标不是功能多，而是跑通：

- 服务注册
- 服务发现
- 网关路由
- 服务间调用
- 配置中心
- 统一鉴权

## 阶段十二：网关统一认证与微服务权限

目标：把单体里的安全模型迁移到微服务架构下。

### 1. 网关统一校验 token

Gateway 负责：

- 校验 `Authorization: Bearer token`
- 放行登录、文档、健康检查接口
- 解析用户 ID
- 把用户信息透传给下游服务

常见透传请求头：

```text
X-User-Id
X-Username
X-Permissions
```

注意：下游服务不能完全相信前端传来的这些头，必须只信任网关透传。

### 2. 下游服务做方法级权限

迁移思路：

- 网关做 token 有效性校验
- 下游服务保留 `@PreAuthorize`
- 权限可以从请求头、缓存或认证上下文中恢复

企业中常见做法：

```text
网关认证
  -> 下游服务鉴权
  -> 前端按钮权限展示
```

三层都要有，不能只靠前端。

### 3. 微服务下的登录退出

需要重新设计：

- token 是否存 Redis
- 退出登录如何让 token 失效
- 修改密码后旧 token 是否失效
- 多端登录是否互踢
- token 续期策略

这些都适合在 Redis 阶段之后做。

## 阶段十三：微服务稳定性与可观测性

目标：从“能调用”升级到“能在线上定位问题”。

### 1. Sentinel 限流与熔断

优先场景：

- 登录接口限流
- 用户分页接口限流
- 文件上传接口限流
- 下游服务异常时熔断降级

需要掌握：

- QPS 限流
- 并发线程数限流
- 慢调用比例
- 异常比例
- fallback 返回

### 2. 统一日志

单体阶段先做好：

- 请求日志
- 操作日志
- 异常日志
- SQL 日志按环境开关

微服务阶段继续补：

- traceId
- 请求链路日志
- 网关访问日志
- 服务名、实例 ID

### 3. 链路追踪

后续可学习：

- Micrometer Tracing
- OpenTelemetry
- Zipkin / Tempo

目标是能回答：

```text
一次请求经过了哪些服务？
哪个服务慢？
哪个服务报错？
```

### 4. 健康检查与监控

学习内容：

- Spring Boot Actuator
- 健康检查接口
- Prometheus
- Grafana

先掌握基础健康检查，再进入完整监控体系。

## 阶段十四：容器化与线上部署

目标：把单体和微服务都能用容器方式跑起来。

### 1. 单体 Docker 化

内容：

- 编写 Dockerfile
- 构建镜像
- 运行容器
- 外部化配置
- 挂载日志目录

### 2. Docker Compose 编排

单体阶段：

```text
admin-system
mysql
redis
```

微服务阶段：

```text
gateway-service
auth-service
system-service
nacos
mysql
redis
```

### 3. 生产部署基础

需要掌握：

- 环境变量
- 配置文件隔离
- 日志目录
- 端口映射
- 服务重启策略
- 简单备份恢复

### 4. CI/CD 入门

学习内容：

- GitHub Actions
- 自动编译
- 自动测试
- 自动构建 Docker 镜像
- 自动部署到服务器

先做到“提交代码后自动打包”，再追求完整发布流水线。

## 暂缓内容

这些不是不学，而是放在当前权限后台系统闭环之后：

- 电商项目
- 复杂分布式事务
- Kubernetes
- Service Mesh
- 高并发秒杀

原因：当前权限后台系统还没闭环。先把一个企业后台做扎实，比同时做两个半成品项目更有价值。

## 推荐最近四周节奏

### 第 1 周：脚手架收口 + 用户模块闭环

- 参数校验
- 错误码
- 用户详情
- 启用禁用
- 重置密码
- 修改密码
- 构建测试整理

### 第 2 周：角色模块 + 用户分配角色

- 角色 CRUD
- 角色分页
- 角色状态
- 用户角色查询
- 用户分配角色

### 第 3 周：菜单权限 + 动态菜单

- 菜单树
- 菜单 CRUD
- 角色分配权限
- 当前用户信息
- 当前用户菜单
- 前后端权限联调思路

### 第 4 周：企业通用能力入门

- 验证码
- 操作日志
- 文件上传下载
- Excel 导入导出
- Redis 缓存验证码或用户权限

## 评价标准

每完成一个阶段，都用下面标准检查：

- 是否有清晰 DTO / VO / Entity 边界
- 是否有权限 key
- 是否有参数校验
- 是否有事务保护
- 是否不返回敏感字段
- 是否能用 curl 或 Knife4j 测通
- 是否更新数据库文档和 README

如果这些都能做到，项目就不是简单练手 CRUD，而是在往企业级后台系统靠近。

## 最终路线总览

完整路线建议如下：

```text
1. 脚手架基础收口
2. 用户模块闭环
3. 角色模块
4. 用户分配角色
5. 菜单权限模块
6. 角色分配权限
7. 登录后用户信息与动态菜单
8. 企业通用能力
9. Redis / RabbitMQ / 定时任务
10. 单体项目部署与工程化
11. 微服务架构入门
12. 网关统一认证与微服务权限
13. 微服务稳定性与可观测性
14. 容器化与线上部署
```

这条路线的核心思想是：先把单体企业后台做完整，再自然演进到微服务。这样学习曲线更稳，项目也更像真实公司里的系统演进过程。
