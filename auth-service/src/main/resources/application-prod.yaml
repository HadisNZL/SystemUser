spring:
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:nacos:8848}
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}

jwt:
  secret: ${JWT_SECRET:abc123456abc123456abc123456abc123456}
  expire: ${JWT_EXPIRE:7200}

springdoc:
  api-docs:
    enabled: false

logging:
  level:
    root: warn
    com.system.auth: info
