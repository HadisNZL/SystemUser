spring:
  cloud:
    nacos:
      discovery:
        server-addr: nacos:8848
    sentinel:
      datasource:
        degrade-rules:
          nacos:
            server-addr: nacos:8848
            data-id: auth-service-sentinel-degrade-rules.json
            group-id: DEFAULT_GROUP
            rule-type: degrade
            data-type: json
  data:
    redis:
      host: redis
      port: 6379

logging:
  level:
    root: info
    com.system.auth: info
